% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{elected_cache_dir}
\alias{elected_cache_dir}
\alias{diretorio_cache_eleitos}
\title{Cache directory}
\usage{
elected_cache_dir(path = NULL)

diretorio_cache_eleitos(caminho = NULL)
}
\arguments{
\item{path}{Optional directory; when given, it is returned (created if
needed) instead of the default resolution.}

\item{caminho}{Portuguese alias of \code{path}.}
}
\value{
The cache directory path, created if needed, invisibly.
}
\description{
Directory where the yearly election files, the parliamentary tables and
their snapshots are stored. By default it is a folder under the session's
\code{\link[=tempdir]{tempdir()}}, so nothing persists between sessions and nothing is written
outside the temporary directory unless you opt in. To keep the files
between sessions, set the environment variable \code{ELECTEDBR_CACHE_DIR} or the
option \code{electedBR.cache_dir} (for example to
\code{tools::R_user_dir("electedBR", "cache")}), or pass \code{cache_dir} to each
function. The precedence is: the \code{cache_dir} argument, then the
environment variable, then the option, then \code{\link[=tempdir]{tempdir()}}.
}
\details{
The parliamentary functions also keep a snapshot of every completed
collection under \verb{cache_dir/snapshots/}; with a persistent cache these
accumulate and can be removed with \code{\link[=elected_clear_cache]{elected_clear_cache()}}.
}
\examples{
elected_cache_dir()
old <- Sys.getenv("ELECTEDBR_CACHE_DIR", unset = NA)
Sys.setenv(ELECTEDBR_CACHE_DIR = file.path(tempdir(), "electedBR-persistent"))
elected_cache_dir()
if (is.na(old)) Sys.unsetenv("ELECTEDBR_CACHE_DIR") else
  Sys.setenv(ELECTEDBR_CACHE_DIR = old)
}
