% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parliament.R
\name{get_deputies}
\alias{get_deputies}
\alias{get_senators}
\alias{consultar_deputados}
\alias{consultar_senadores}
\title{Federal deputies and senators currently serving}
\usage{
get_deputies(
  state = NULL,
  party = NULL,
  status = "serving",
  role = NULL,
  refresh = FALSE,
  max_age_hours = 6,
  cache_dir = tools::R_user_dir("electedBR", "cache")
)

get_senators(
  state = NULL,
  party = NULL,
  status = "serving",
  role = NULL,
  refresh = FALSE,
  max_age_hours = 6,
  cache_dir = tools::R_user_dir("electedBR", "cache")
)

consultar_deputados(
  uf = NULL,
  partido = NULL,
  situacao = "em_exercicio",
  condicao = NULL,
  atualizar = FALSE,
  validade_horas = 6,
  cache_dir = tools::R_user_dir("electedBR", "cache")
)

consultar_senadores(
  uf = NULL,
  partido = NULL,
  situacao = "em_exercicio",
  condicao = NULL,
  atualizar = FALSE,
  validade_horas = 6,
  cache_dir = tools::R_user_dir("electedBR", "cache")
)
}
\arguments{
\item{state}{Two-letter state abbreviations; \code{NULL} keeps every state.}

\item{party}{Party abbreviations (current affiliation).}

\item{status}{Only \code{"serving"} is supported.}

\item{role}{\code{NULL}, or one or more of \code{"principal"}, \code{"alternate"} and
\code{"unknown"}.}

\item{refresh}{Logical. Collect again even if a fresh cache exists.}

\item{max_age_hours}{Maximum age of the cache, in hours (default six).}

\item{cache_dir}{Cache directory; defaults to
\code{tools::R_user_dir("electedBR", "cache")}.}

\item{uf, partido, situacao, condicao, atualizar, validade_horas}{Portuguese
aliases of \code{state}, \code{party}, \code{status} (\code{"em_exercicio"}), \code{role}
(\code{"titular"}, \code{"suplente"}, \code{"desconhecido"}), \code{refresh} and
\code{max_age_hours}.}
}
\value{
A tibble with the columns \code{person_id}, \code{source_id}, \code{name}, \code{state},
\code{office}, \code{current_party}, \code{mandate_id}, \code{mandate_role},
\code{mandate_role_raw}, \code{exercise_status}, \code{exercise_status_raw},
\code{exercise_start}, \code{exercise_end}, \code{status_recorded_at},
\code{source_updated_at}, \code{source}, \code{reference} and \code{retrieved_at}. Columns
ending in \verb{_raw} keep the label used by the source; \code{person_id} is
namespaced (\code{camara:204379}, \code{senado:5322}) and is not a TSE identifier.
}
\description{
Retrieves the members of the Chamber of Deputies and of the Federal Senate
currently in service, from the open data APIs of each house. The lists
include alternates who are serving; \code{mandate_role} tells principals and
alternates apart, independently of the service status. Election results are
a different question, answered by \code{\link[=get_elected]{get_elected()}}.
}
\details{
The Chamber list is paginated and the electoral condition of each deputy
comes from a detail request per deputy, so the first call without \code{state}
issues several hundred requests; results are cached for \code{max_age_hours}
hours in \code{cache_dir}, and every completed collection is also kept as an
immutable snapshot under \verb{cache_dir/snapshots/}. A network or schema failure
raises an error rather than silently returning expired data.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
get_senators(state = "PE", cache_dir = tempdir())
get_deputies(state = c("PE", "PB"), role = "alternate", cache_dir = tempdir())
consultar_senadores(uf = "PE", cache_dir = tempdir())
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_service_history]{get_service_history()}} for the status records and service
periods of one member.
}
