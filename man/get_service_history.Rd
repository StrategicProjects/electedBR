% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/history.R
\name{get_service_history}
\alias{get_service_history}
\alias{consultar_historico_exercicio}
\title{Service history of a deputy or senator}
\usage{
get_service_history(
  person_id,
  refresh = FALSE,
  max_age_hours = 6,
  cache_dir = tools::R_user_dir("electedBR", "cache")
)

consultar_historico_exercicio(
  id_pessoa,
  atualizar = FALSE,
  validade_horas = 6,
  cache_dir = tools::R_user_dir("electedBR", "cache")
)
}
\arguments{
\item{person_id}{A single id such as \code{"camara:204379"} or \code{"senado:5322"}.}

\item{refresh}{Logical. Collect again even if a fresh cache exists.}

\item{max_age_hours}{Maximum age of the cache, in hours (default six).}

\item{cache_dir}{Cache directory; defaults to
\code{tools::R_user_dir("electedBR", "cache")}.}

\item{id_pessoa, atualizar, validade_horas}{Portuguese aliases of \code{person_id},
\code{refresh} and \code{max_age_hours}.}
}
\value{
A tibble with the columns \code{person_id}, \code{name}, \code{state}, \code{office},
\code{mandate_id}, \code{mandate_role}, \code{mandate_role_raw}, \code{exercise_status},
\code{exercise_status_raw}, \code{party_at_record}, \code{record_type}, \code{record_at},
\code{exercise_start}, \code{exercise_end}, \code{description}, \code{source},
\code{source_updated_at} and \code{retrieved_at}.
}
\description{
Returns the official records about the service of one member of Congress,
identified by the \code{person_id} returned by \code{\link[=get_deputies]{get_deputies()}} or
\code{\link[=get_senators]{get_senators()}}. The two houses publish different things and the
difference is preserved rather than reconciled:
}
\details{
\itemize{
\item Chamber (\verb{camara:}): \code{record_type = "status_record"}, one row per status
record with \code{record_at}, the situation and the party at that moment.
Registry or party changes are not turned into starts or ends of service,
so \code{exercise_start} and \code{exercise_end} are \code{NA}.
\item Senate (\verb{senado:}): \code{record_type = "service_period"}, one row per official
period of each mandate, with \code{exercise_start} and \code{exercise_end}. An open
period is not by itself evidence of current service; use \code{\link[=get_senators]{get_senators()}}
for that.
}
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
senators <- get_senators(state = "PE", cache_dir = tempdir())
get_service_history(senators$person_id[[1]], cache_dir = tempdir())
get_service_history("camara:204379", cache_dir = tempdir())
\dontshow{\}) # examplesIf}
}
