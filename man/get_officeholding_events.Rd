% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{get_officeholding_events}
\alias{get_officeholding_events}
\alias{consultar_eventos_exercicio}
\title{Office-holding events for elected officials}
\usage{
get_officeholding_events(
  events = NULL,
  refresh = FALSE,
  max_age_hours = 24,
  cache_dir = elected_cache_dir(),
  base_url = getOption("electedBR.base_url")
)

consultar_eventos_exercicio(
  eventos = NULL,
  atualizar = FALSE,
  validade_horas = 24,
  cache_dir = elected_cache_dir(),
  base_url = getOption("electedBR.base_url")
)
}
\arguments{
\item{events}{Optional data frame with the same columns, used instead of
downloading (for example a local copy under review).}

\item{refresh}{Logical. Collect again even if a fresh cache exists.}

\item{max_age_hours}{Maximum age of the cache, in hours (default six).}

\item{cache_dir}{Cache directory; see \code{\link[=elected_cache_dir]{elected_cache_dir()}}.}

\item{base_url}{Optional base URL of a mirror hosting the files listed in
\link{elected_years}. Defaults to \code{getOption("electedBR.base_url")}; when
\code{NULL}, the \code{url} column of \link{elected_years} is used.}

\item{eventos, atualizar, validade_horas}{Portuguese aliases of \code{events},
\code{refresh} and \code{max_age_hours}.}
}
\value{
A tibble with the columns \code{year} and \code{candidate_id} (identifying
the official in the yearly file of that election), \code{name}, \code{office},
\code{state}, \code{municipality_tse_id}, \code{event} (\code{resignation}, \code{death},
\code{removal}, \code{leave} or \code{return}), \code{date}, \code{successor_candidate_id},
\code{successor_name}, \code{successor_date} (when the successor took office),
\code{source} (URL of an official or press record) and \code{notes}, plus
\code{retrieved_at}.
}
\description{
A curated table of changes in office holding after the election (an
official who resigned, died, was removed or took leave, and who succeeded
them), for the offices covered by \code{\link[=get_elected]{get_elected()}} that have no official
API of sitting members: mayors, governors and their deputies. It is
maintained in the package repository, served from the same host as the
yearly files and refreshed on demand, not on a schedule; an official absent
from the table has simply not been recorded, which is not evidence of
being in office. Every row cites its source. Contributions are welcome at
\url{https://github.com/StrategicProjects/electedBR}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
get_officeholding_events(cache_dir = tempdir())
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_elected]{get_elected()}}, whose \code{as_of} argument applies these events.
}
